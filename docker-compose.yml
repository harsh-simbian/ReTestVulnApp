version: '3.8'

services:
  xss-demo:
    build: .
    ports:
      - "9090:9090"
    container_name: xss-vulnerability-demo
    environment:
      - NODE_ENV=production
      - PORT=9090
    volumes:
      - ./state.json:/app/state.json
    restart: unless-stopped
